indent-width = 4
line-length = 99
target-version = "py314"

[lint]
select = [
    "E",  # pycodestyle
    "F",  # Pyflakes + flake8
    "I",  # isort
    "UP", # pyupgrade
    "W",  # pycodestyle
]
ignore = [
    "E501",   # Line too long; TODO drop
    "UP031",  # Use format specifiers instead of percent format; TODO drop
]
